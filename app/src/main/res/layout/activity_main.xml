<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nion_background"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="4dp">

        <Button
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="←" android:contentDescription="Back"
            />

        <Button
            android:id="@+id/forwardButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="→" android:contentDescription="Forward"
            />

        <Button
            android:id="@+id/reloadButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:text="↻" android:contentDescription="Reload page"
            />

        <EditText
            android:id="@+id/addressBar"
            android:contentDescription="Address or search"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:hint="Waiting for Tor..."
            android:imeOptions="actionGo"
            android:inputType="textUri"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:singleLine="true"
            android:textColor="@color/nion_text_primary"
            android:textColorHint="@color/nion_text_secondary" />

        <Button
            android:id="@+id/goButton"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="Go" android:contentDescription="Go to address"
            />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal">

        <HorizontalScrollView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:fillViewport="true"
            android:scrollbars="none">

            <LinearLayout
                android:id="@+id/tabStrip"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="horizontal" />

        </HorizontalScrollView>

        <Button
            android:id="@+id/siteInfoButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:visibility="gone"
            android:contentDescription="Site information"
            android:text="ⓘ" />

        <Button
            android:id="@+id/clearSiteDataButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:visibility="gone"
            android:contentDescription="Clear data for this site"
            android:text="⌫" />

        <Button
            android:id="@+id/bookmarkButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="Bookmark"
            android:text="☆" />

        <Button
            android:id="@+id/closeTabButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="×" android:contentDescription="Close tab"
            />

        <Button
            android:id="@+id/newTabButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="+" android:contentDescription="New tab"
            />
        <Button
            android:id="@+id/browserMenuButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="Browser menu"
            android:text="⋮" />

    </LinearLayout>

    <TextView
        android:id="@+id/torStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="10dp"
        android:paddingTop="5dp"
        android:paddingEnd="10dp"
        android:paddingBottom="5dp"
        android:text="Tor: starting..."
        android:textColor="@color/nion_text_secondary"
        android:textSize="12sp" />
    <Button
        android:id="@+id/retryTorButton"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_gravity="end"
        android:layout_marginEnd="8dp"
        android:contentDescription="Retry Tor connection"
        android:text="Retry Tor"
        android:visibility="gone" />


    <ProgressBar
        android:id="@+id/pageProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:max="100"
        android:progress="0"
        android:visibility="gone" />

    <org.mozilla.geckoview.GeckoView
        android:id="@+id/geckoview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
